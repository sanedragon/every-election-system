package elections

import org.scalatest._

class PreferenceMatrixSpec extends FlatSpec with Matchers {
  "A PreferenceMatrix" should "get tabulated correctly from a trivial set of ranked ballots" in {
    "this test" should be ("written")
  }

  it should "get tabulated correctly from a trivial set of scored ballots" in {
    "this test" should be ("written")
  }

  it should "get the positive preferences correctly" in {
    "this test" should be ("written")
  }
}
